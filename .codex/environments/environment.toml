# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "praxisplaner"

[setup]
script = '''
set -eu

SOURCE_ENV="$HOME/Code/praxisplaner/.env.local"

[ -f "$SOURCE_ENV" ] || { echo "Missing $SOURCE_ENV"; exit 1; }

cp -f "$SOURCE_ENV" .env.local
chmod 600 .env.local

export CONVEX_AGENT_MODE=anonymous
pnpm i
pnpm gen
'''

[[actions]]
name = "Verify CI checks don't fail."
icon = "tool"
command = '''
pnpm gen
pnpm lint
pnpm tsc
pnpm format
pnpm test
pnpm build
'''
